<% content_for :title, "All Recipes" %>
<h1>Recipes</h1>

<div class="recipe-grid">
  <% @recipes.each do |recipe| %>
    <%= link_to recipe_path(recipe), style: "text-decoration:none; color:inherit;" do %>
      <div class="recipe-card">
        <% if recipe.cover_image.present? %>
          <%= image_tag recipe.cover_image.thumb.url, alt: recipe.title, class: "recipe-card-image" %>
        <% else %>
          <div class="recipe-card-image">🍽️</div>
        <% end %>
        <div class="recipe-card-body">
          <p class="recipe-card-title"><%= recipe.title %></p>
          <p class="recipe-card-meta">by <%= recipe.user.username %> · Serves <%= recipe.base_servings %></p>
        </div>
      </div>
    <% end %>
  <% end %>
</div>

<% if @recipes.empty? %>
  <p style="text-align:center; color:var(--text-secondary); margin-top:3rem;">
    No recipes yet. <%= link_to "Add one!", new_recipe_path, style: "color:var(--brand-pink); font-weight:600;" %>
  </p>
<% end %>
